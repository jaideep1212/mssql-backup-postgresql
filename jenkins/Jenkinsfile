pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps { checkout scm }
        }
        stage('Smoke test') {
            steps { echo 'Pipeline wired up correctly.' }
        }
    }
}